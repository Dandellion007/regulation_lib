<% content_for :content do %>
  <div class="jumbotron">
    <h3><%= content_for?(:sub_title) ? yield(:sub_title) : "Дополнительная страница" %></h3>
  </div>

  <div class="container">
    <%= yield %>
  </div>
<% end %>

<%= render template: "layouts/application" %>

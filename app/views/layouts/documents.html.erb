<% content_for :content do %>
  <div class="jumbotron text-center">
    <h1><%= content_for?(:sub_title) ? yield(:sub_title) : "Sub Title" %></h1>
  </div>

  <div class="container">
    <%= yield %>
  </div>
<% end %>

<%= render template: "layouts/application" %>
